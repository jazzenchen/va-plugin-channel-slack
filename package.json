{
  "name": "@vibearound/plugin-channel-slack",
  "version": "0.6.2",
  "private": true,
  "type": "module",
  "description": "VibeAround Slack channel plugin (stdio JSON-RPC)",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc",
    "test": "npm run build && node --test test/*.test.mjs",
    "dev": "tsc --watch",
    "start": "node dist/main.js"
  },
  "dependencies": {
    "@slack/bolt": "^4.1.0",
    "@vibearound/plugin-channel-sdk": "file:../va-plugin-channel-sdk"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.7.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
